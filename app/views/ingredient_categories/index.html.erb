<h1>Listing ingredient_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ingredient_categories.each do |ingredient_category| %>
  <tr>
    <td><%= ingredient_category.name %></td>
    <td><%= ingredient_category.description %></td>
    <td><%= link_to 'Show', ingredient_category %></td>
    <td><%= link_to 'Edit', edit_ingredient_category_path(ingredient_category) %></td>
    <td><%= button_to 'Destroy', ingredient_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_ingredient_category_path %>
